<!doctype html>
<html">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Hello, world!</title>
  </head>
  <body>
    <%- include( './component/header.ejs' ) %>
    <!-- 
      @Param results : service.js에서 가져온 db에 저장된 데이터들
      EJS( npm install ejs 설치 ) 
      기본 사용법 : <%= 'data' %>
    -->
    <div class="container">
      <% for( const res of results ) { %>
          <h4>할 일 : <%= res.subject %></h4>
          <p>기간 : <%= res.dueDate %></p>
      <%  } %>
    </div>
  </body>
</html>